'use client';

// Work panels built one-by-one after cosmic journey is approved.
// Returns a black canvas until each scene is implemented.
export default function PanelHost({ activeIndex: _ }: { activeIndex: number }) {
  return <div style={{ width: '100%', height: '100%', background: '#000' }} />;
}
